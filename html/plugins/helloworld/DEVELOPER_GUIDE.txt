================================================================================
FLINTMANCMS PLUGIN DEVELOPMENT GUIDE
Hello World Plugin - Complete Documentation
================================================================================

TABLE OF CONTENTS
================================================================================
1. Introduction
2. Quick Start
3. Plugin Architecture
4. File-by-File Breakdown
5. Database Integration
6. Frontend Development
7. Admin Interface
8. Security Best Practices
9. Testing Your Plugin
10. Deployment Checklist
11. Troubleshooting
12. Advanced Topics

================================================================================
1. INTRODUCTION
================================================================================

The Hello World plugin is a comprehensive template demonstrating all aspects
of FlintmanCMS plugin development. It includes:

- Database table creation and management
- CRUD operations (Create, Read, Update, Delete)
- Frontend user interface
- Admin management interface
- Custom CSS styling
- JavaScript interactivity
- Smarty template integration
- Security measures
- Permission handling

This plugin serves as both a working example and a template for creating your
own custom plugins.

================================================================================
2. QUICK START
================================================================================

INSTALLATION:
-------------
1. Copy the entire /helloworld/ folder to /plugins/
2. Log in as administrator
3. Navigate to: admin.php?n=plugins
4. Click "Activate" next to "Hello World"
5. System will automatically:
   - Create database tables (from sql/install.sql)
   - Add menu link to navigation
   - Enable the plugin

ACCESSING THE PLUGIN:
--------------------
Frontend: index.php?n=plugins&p=helloworld
Admin:    admin.php?n=plugins&p=helloworld

TESTING:
-------
1. Visit frontend - see list of sample messages
2. Click "Add New Message" - submit a message
3. View message details
4. Access admin panel - manage messages
5. Try Edit, Delete, Settings operations

================================================================================
3. PLUGIN ARCHITECTURE
================================================================================

DIRECTORY STRUCTURE:
-------------------
/plugins/helloworld/
│
├── variable.php          # Plugin metadata & table definitions
├── helloworld.php        # Main frontend logic
├── README.txt            # Architecture overview
├── DEVELOPER_GUIDE.txt   # This file
├── index.html            # Security: block directory listing
│
├── /admin/
│   ├── admin.php         # Admin CRUD operations
│   └── index.html        # Security
│
├── /sql/
│   ├── install.sql       # Database schema
│   └── index.html        # Security
│
├── /template/
│   ├── page.htm          # Smarty admin template
│   └── index.html        # Security
│
├── /css/
│   ├── helloworld.css    # Plugin styles
│   └── index.html        # Security
│
└── /js/
    ├── helloworld.js     # Plugin JavaScript
    └── index.html        # Security

EXECUTION FLOW:
--------------
Frontend Access (index.php?n=plugins&p=helloworld):
1. User requests URL
2. main plugins.php checks if plugin exists
3. Loads helloworld.php
4. helloworld.php:
   - Checks permissions (authentication.php)
   - Processes actions (view, add, list)
   - Queries database
   - Builds HTML content
   - Assigns to Smarty
5. CMS renders with header/footer

Admin Access (admin.php?n=plugins&p=helloworld):
1. Admin requests URL
2. main plugin routing loads admin/admin.php
3. admin.php:
   - Checks admin authentication
   - Processes actions (add, edit, delete, settings, list)
   - Builds forms or tables
   - Assigns to Smarty
4. template/page.htm wraps content
5. CMS renders with admin layout

================================================================================
4. FILE-BY-FILE BREAKDOWN
================================================================================

variable.php
-----------
PURPOSE: Defines plugin metadata and database requirements
LOADED: During activation, deactivation, info display
KEY VARIABLES:
  - $plugin_db_tables: Array of table names (without prefix)
  - $plugin_name: Display name
  - $plugin_description: What the plugin does
  - $plugin_version: Version number (major.minor.patch)
  - $plugin_folder: Directory name (must match actual folder)

EXAMPLE:
  $plugin_db_tables = array("helloworld_messages", "helloworld_settings");
  $plugin_version = "1.0.0";

WHEN TO MODIFY:
  - Adding/removing database tables
  - Changing plugin name or description
  - Updating version after changes
  - Adding custom configuration variables

---

sql/install.sql
--------------
PURPOSE: Creates database tables when plugin is activated
LOADED: Automatically during first activation
SYNTAX RULES:
  - Use `flintmancms_` for table names
  - End each statement with semicolon
  - Comments start with --
  - All tables must be in $plugin_db_tables array

EXAMPLE:
  CREATE TABLE `flintmancms_helloworld_messages` (
    `id` int(11) NOT NULL AUTO_INCREMENT,
    `message` varchar(255) NOT NULL,
    PRIMARY KEY (`id`)
  ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

TABLE DESIGN:
  - Always use AUTO_INCREMENT integer primary key named 'id'
  - Add indexes on frequently queried columns
  - Use utf8mb4 for full Unicode support
  - Provide default values where appropriate
  - Use datetime for timestamps

---

helloworld.php (Frontend)
-------------------------
PURPOSE: Handles frontend display and user interactions
URL: index.php?n=plugins&p=helloworld
STRUCTURE:
  1. Security check (IN_CMS)
  2. Load CSS/JS ($scriptAdd)
  3. Authentication check
  4. Initialize variables
  5. Process actions (view, add, list)
  6. Query database
  7. Build HTML output
  8. Assign to Smarty

COMMON PATTERNS:

Action Processing:
  if ($action == 'view') {
      // Load and display single item
  } elseif ($action == 'add') {
      if (!isset($_POST['submit'])) {
          // Show form
      } else {
          // Process form
      }
  } else {
      // Default: list all items
  }

Database Query:
  $sql = sprintf("SELECT * FROM flintmancms_table WHERE id=%s",
                 quote_smart($id));
  $result = $db->sql_query($sql);
  $data = $db->sql_fetchrow($result);

Form Submission:
  $value = scrub_input($_POST['field']);
  $sql = sprintf("INSERT INTO table VALUES (%s)", quote_smart($value));
  $db->sql_query($sql);
  header("Location: redirect_url");
  exit;

---

admin/admin.php
--------------
PURPOSE: Administrative interface for CRUD operations
URL: admin.php?n=plugins&p=helloworld
STRUCTURE:
  1. Security check
  2. Admin authentication
  3. Initialize variables
  4. Process actions (add, edit, delete, settings, list)
  5. Build forms or use Report class for tables
  6. Assign to Smarty template

CRUD PATTERN:

List (default):
  - Query all records
  - Use Report class for table display
  - Include action links (edit, delete)

Add:
  - Step 1: Display empty form
  - Step 2: Process submission, redirect

Edit:
  - Step 1: Load data, display filled form
  - Step 2: Process update, redirect

Delete:
  - Step 1: Display confirmation
  - Step 2: Perform deletion, redirect

REPORT CLASS:
  $report->setMainAttributes('width="100%"');
  $report->addOutputColumn('field', 'Label', 'align', 'width');
  $report->display($data_array);
  $content = $report->showReport();

---

template/page.htm
----------------
PURPOSE: Smarty template for admin interface
VARIABLES RECEIVED:
  - {$form_action}: Where form submits
  - {$content}: HTML content from admin.php
  - {$csrf_token}: CSRF protection token (automatically available)

STRUCTURE:
  {if $form_action != ''}
      <form action="{$form_action}" method="post">
          <input type="hidden" name="csrf_token" value="{$csrf_token}">
          {$content}
      </form>
  {else}
      {$content}
  {/if}

IMPORTANT: The csrf_token hidden field is REQUIRED for security.
Every form that submits data MUST include this token.

WHEN TO CUSTOMIZE:
  - Add plugin-specific wrapper HTML
  - Include JavaScript for validation
  - Add custom CSS classes
  - Modify form structure

---

css/helloworld.css
-----------------
PURPOSE: Plugin-specific styles
LOADED: Via $scriptAdd in helloworld.php
BEST PRACTICES:
  - Use CSS variables for theme compatibility
  - Prefix class names with plugin name
  - Provide responsive styles
  - Include dark mode support

EXAMPLE:
  .hello-message-card {
      background: var(--card-bg, #fff);
      border: 1px solid var(--border-color, #ddd);
  }

---

js/helloworld.js
---------------
PURPOSE: Plugin-specific JavaScript
LOADED: Via $scriptAdd in helloworld.php
FEATURES:
  - Form validation
  - Interactive UI
  - AJAX operations (optional)
  - Event handling

INITIALIZATION:
  document.addEventListener('DOMContentLoaded', function() {
      // Your code here
  });

================================================================================
5. DATABASE INTEGRATION
================================================================================

WORKFLOW:
--------
1. Define tables in variable.php $plugin_db_tables array (without prefix)
2. Create schema in sql/install.sql (with flintmancms_ prefix)
3. System creates tables during activation
4. Access via $db object in PHP files

QUERY PATTERNS:
--------------

SELECT (Single Row):
  $sql = sprintf("SELECT * FROM flintmancms_table WHERE id=%s",
                 quote_smart($id));
  $result = $db->sql_query($sql);
  $data = $db->sql_fetchrow($result);
  if ($data) {
      echo $data['column'];
  }

SELECT (Multiple Rows):
  $sql = "SELECT * FROM flintmancms_table ORDER BY date DESC";
  $result = $db->sql_query($sql);
  while ($row = $db->sql_fetchrow($result)) {
      echo $row['column'];
  }

COUNT:
  $count = $db->sql_numrows($result);

INSERT:
  $sql = sprintf("INSERT INTO flintmancms_table (col1, col2) " .
                 "VALUES (%s, %s)",
                 quote_smart($val1),
                 quote_smart($val2));
  $db->sql_query($sql);

UPDATE:
  $sql = sprintf("UPDATE flintmancms_table SET col=%s WHERE id=%s",
                 quote_smart($value),
                 quote_smart($id));
  $db->sql_query($sql);

DELETE:
  $sql = sprintf("DELETE FROM flintmancms_table WHERE id=%s",
                 quote_smart($id));
  $db->sql_query($sql);

ERROR HANDLING:
  if (!$result) {
      $errorMsg = "Database error: " . $db->sql_error();
  }

SECURITY RULES:
--------------
1. ALWAYS use quote_smart() for user input in SQL
2. NEVER directly concatenate user input into SQL
3. Use sprintf() with quote_smart() for parameterized queries
4. Sanitize input with scrub_input() before using

WRONG:
  $sql = "SELECT * FROM table WHERE id=" . $_GET['id'];  // SQL INJECTION!

CORRECT:
  $id = scrub_input($_GET['id']);
  $sql = sprintf("SELECT * FROM table WHERE id=%s", quote_smart($id));

================================================================================
6. FRONTEND DEVELOPMENT
================================================================================

BUILDING USER INTERFACES:
-------------------------

HTML Generation:
  $body = '';
  $body .= '<div class="container">';
  $body .= '<h2>' . htmlspecialchars($title) . '</h2>';
  $body .= '</div>';

Forms:
  $body .= '<form method="POST" action="URL">';
  $body .= '<input type="text" name="field" class="form-control" required>';
  $body .= '<button type="submit" name="submit" class="btn btn-primary">';
  $body .= 'Submit</button>';
  $body .= '</form>';

Links:
  $body .= '<a href="index.php?n=plugins&p=helloworld&action=view&id=' .
           $id . '" class="button">View</a>';

Loops:
  while ($row = $db->sql_fetchrow($result)) {
      $body .= '<div class="card">';
      $body .= htmlspecialchars($row['data']);
      $body .= '</div>';
  }

SECURITY IN OUTPUT:
------------------
ALWAYS use htmlspecialchars() when outputting user data:
  $body .= htmlspecialchars($user_input);

This prevents XSS (Cross-Site Scripting) attacks.

STYLING:
-------
Use CSS classes compatible with CMS themes:
  - .btn, .btn-primary: Buttons
  - .button: Secondary button style
  - .form-control: Input fields
  - .form-group: Form field container

Add plugin-specific styles in css/helloworld.css

SMARTY ASSIGNMENT:
-----------------
  $smarty->assign('text', $page_title);
  $smarty->assign('body', $html_content);

These appear in {$text} and {$body} in templates.

================================================================================
7. ADMIN INTERFACE
================================================================================

AUTHENTICATION:
--------------
Add this at the top of admin.php:
  array_push($page_lvl, 'Admin');
  include(INCLUDES_PATH . 'authentication.php');

This ensures only admins can access.

CRUD OPERATIONS:
---------------

LIST:
Create an array of data, use Report class:
  $items = array();
  while ($row = $db->sql_fetchrow($result)) {
      array_push($items, array(
          'id' => $row['id'],
          'name' => $row['name'],
          'edit' => '<a href="...&action=edit&id=' . $row['id'] . '">Edit</a>',
          'delete' => '<a href="...&action=delete&id=' . $row['id'] . '">Delete</a>'
      ));
  }

  $report->addOutputColumn('id', 'ID', 'left');
  $report->addOutputColumn('name', 'Name', 'left');
  $report->addOutputColumn('edit', '', 'center');
  $report->addOutputColumn('delete', '', 'center');
  $report->display($items);
  $content = $report->showReport();

ADD:
  if (!isset($_POST['submit'])) {
      // Build form
      $form_action = "admin.php?n=plugins&p=helloworld&action=add";
      $content = '<input type="text" name="field" class="form-control">';
      $content .= '<input type="submit" name="submit" value="Save">';
  } else {
      // IMPORTANT: ALWAYS verify CSRF token first!
      if (!isset($_POST['csrf_token']) || !verify_csrf_token($_POST['csrf_token'])) {
          die("CSRF token validation failed");
      }
      // Process form
      $value = scrub_input($_POST['field']);
      // Insert to database
      header("Location: admin.php?n=plugins&p=helloworld");
      exit;
  }

EDIT:
Similar to ADD, but load existing data first:
  $sql = sprintf("SELECT * FROM table WHERE id=%s", quote_smart($id));
  $result = $db->sql_query($sql);
  $data = $db->sql_fetchrow($result);

  if (!isset($_POST['submit'])) {
      $content = '<input type="text" name="field" value="' .
                 htmlspecialchars($data['field']) . '">';
  } else {
      // IMPORTANT: ALWAYS verify CSRF token first!
      if (!isset($_POST['csrf_token']) || !verify_csrf_token($_POST['csrf_token'])) {
          die("CSRF token validation failed");
      }
      // Process update...
  }

DELETE:
Show confirmation, then delete:
  if (!isset($_POST['submit'])) {
      $content = 'Are you sure?';
      $content .= '<input type="submit" name="submit" value="Delete">';
  } else {
      // IMPORTANT: ALWAYS verify CSRF token first!
      if (!isset($_POST['csrf_token']) || !verify_csrf_token($_POST['csrf_token'])) {
          die("CSRF token validation failed");
      }
      $sql = sprintf("DELETE FROM table WHERE id=%s", quote_smart($id));
      $db->sql_query($sql);
      header("Location: ...");
      exit;
  }

================================================================================
8. SECURITY BEST PRACTICES
================================================================================

1. FILE ACCESS CONTROL:
   - Check IN_CMS constant at top of every PHP file
   - Add index.html to every directory

   if (!defined('IN_CMS')) {
       die("ERROR - Direct access not permitted");
   }

2. INPUT SANITIZATION (CRITICAL):
   - Use scrub_input() for ALL user input from $_GET, $_POST, $_COOKIE
   - Enhanced with type validation and length limits

   Basic usage (backward compatible):
   $id = scrub_input($_GET['id']);
   $name = scrub_input($_POST['name']);

   Enhanced validation (RECOMMENDED):
   $email = scrub_input($_POST['email'], ['type' => 'email']);
   $age = scrub_input($_POST['age'], ['type' => 'int']);
   $website = scrub_input($_POST['url'], ['type' => 'url']);
   $bio = scrub_input($_POST['bio'], ['max_length' => 500]);
   $username = scrub_input($_POST['username'], ['type' => 'alphanum', 'max_length' => 20]);

   Available types:
   - 'text' (default) - strips tags, escapes HTML
   - 'email' - validates email format
   - 'url' - validates URL format
   - 'int' - returns integer or 0
   - 'float' - returns float or 0.0
   - 'alpha' - only letters and spaces
   - 'alphanum' - only letters, numbers, and spaces

   Array handling (automatic):
   $tags = scrub_input($_POST['tags']); // Works with arrays

3. SQL INJECTION PREVENTION:
   - ALWAYS use quote_smart() + sprintf() for SQL parameters
   - Never concatenate user input directly into queries
   - The current approach is SECURE when used consistently

   ❌ WRONG (SQL Injection vulnerability):
   $sql = "SELECT * FROM table WHERE id=" . $_GET['id'];
   $sql = "SELECT * FROM table WHERE name='" . $_POST['name'] . "'";

   ✅ CORRECT (Safe with quote_smart):
   $sql = sprintf("SELECT * FROM flintmancms_table WHERE id=%s",
       quote_smart($id));

   $sql = sprintf("INSERT INTO flintmancms_table (name, email) VALUES (%s, %s)",
       quote_smart($name),
       quote_smart($email));

   Advanced: For NEW code, you can optionally use prepared statements:
   $result = $db->sql_prepare(
       "SELECT * FROM flintmancms_table WHERE id = ? AND status = ?",
       [$id, $status]
   );
   $data = $result->fetch_assoc();

   Benefits of prepared statements:
   - Slightly more secure (true parameter binding)
   - Better performance for repeated queries
   - Cleaner syntax for complex queries

   Note: Both approaches are secure. Use quote_smart() for consistency with
   existing code, or sql_prepare() for new features if you prefer.

4. XSS PREVENTION:
   - Use htmlspecialchars() when outputting user data
   - Escapes HTML special characters

   echo htmlspecialchars($user_input);

5. AUTHENTICATION:
   - Check permissions for sensitive operations
   - Use authentication.php include
   - Verify admin status for admin pages

   array_push($page_lvl, 'Admin');
   include(INCLUDES_PATH . 'authentication.php');

6. FORM VALIDATION:
   - Validate on both client (JS) and server (PHP)
   - Check required fields
   - Verify data types and formats
   - Provide clear error messages

7. CSRF PROTECTION (REQUIRED):
   - ALL forms MUST include CSRF token
   - Verify token on ALL POST handlers
   - Token is automatically available in Smarty templates

   Template (page.htm):
   <form action="{$form_action}" method="post">
       <input type="hidden" name="csrf_token" value="{$csrf_token}">
       <!-- form fields -->
   </form>

   PHP Handler:
   if (isset($_POST['submit'])) {
       // ALWAYS verify CSRF token FIRST
       if (!isset($_POST['csrf_token']) || !verify_csrf_token($_POST['csrf_token'])) {
           die("CSRF token validation failed");
       }
       // Process form data...
   }

   CRITICAL: Every state-changing operation (add/edit/delete) must:
   1. Include csrf_token hidden field in template
   2. Verify token before processing POST data
   3. Use verify_csrf_token() function (available globally)

8. XSS PROTECTION (REQUIRED):
   - ALWAYS escape user-controlled data when outputting to HTML
   - Use htmlspecialchars() for all user input displayed in PHP
   - Never directly output $_GET, $_POST, or database user content without escaping

   WRONG (Vulnerable to XSS):
   $body .= '<div>' . $data['user_input'] . '</div>';
   $body .= '<img alt="' . $_GET['title'] . '">';

   CORRECT (Safe):
   $body .= '<div>' . htmlspecialchars($data['user_input'], ENT_QUOTES, 'UTF-8') . '</div>';
   $body .= '<img alt="' . htmlspecialchars($_GET['title'], ENT_QUOTES, 'UTF-8') . '">';

   Best Practice Pattern:
   // Escape user data immediately when retrieving from database
   $safe_name = htmlspecialchars($data['name'], ENT_QUOTES, 'UTF-8');
   $safe_description = htmlspecialchars($data['description'], ENT_QUOTES, 'UTF-8');

   // Then use the safe variables in HTML
   $body .= '<h2>' . $safe_name . '</h2>';
   $body .= '<p>' . $safe_description . '</p>';

   CRITICAL: Escape data in these contexts:
   - HTML content: htmlspecialchars($data, ENT_QUOTES, 'UTF-8')
   - HTML attributes: htmlspecialchars($data, ENT_QUOTES, 'UTF-8')
   - JavaScript strings: json_encode($data, JSON_HEX_TAG | JSON_HEX_AMP)
   - URLs: urlencode($data)

9. FILE UPLOADS:
   - Use validate_upload() function for comprehensive validation
   - Validate file types with MIME detection (not just extensions)
   - Check file sizes
   - Generate random secure filenames
   - Store with read-only permissions (0644)
   - Add .htaccess to prevent code execution in upload directories

   Example:
   $validation = validate_upload($_FILES['file'], ['image/jpeg', 'image/png'], 5242880);
   if (!$validation['valid']) {
       $errorMsg = $validation['error'];
   } else {
       $filename = $validation['filename']; // Secure random name
       move_uploaded_file($_FILES['file']['tmp_name'], 'uploads/' . $filename);
       chmod('uploads/' . $filename, 0644);
   }

================================================================================
10. TESTING YOUR PLUGIN
================================================================================

FUNCTIONALITY TESTING:
---------------------
Frontend:
  [ ] List view displays correctly
  [ ] Single item view shows details
  [ ] Add form validates required fields
  [ ] Add form submits and saves data
  [ ] Navigation works (back buttons, links)
  [ ] CSS styles apply correctly
  [ ] JavaScript functions work
  [ ] Empty states show appropriate messages

Admin:
  [ ] List table shows all records
  [ ] Add creates new records
  [ ] Edit loads and updates records
  [ ] Delete requires confirmation and removes records
  [ ] Settings save and persist
  [ ] Validation prevents invalid data
  [ ] Error messages display clearly

DATABASE:
  [ ] Tables created on activation
  [ ] Sample data loads correctly
  [ ] Queries execute without errors
  [ ] Indexes improve performance
  [ ] Foreign keys maintain referential integrity

SECURITY TESTING:
----------------
  [ ] Direct PHP file access blocked (IN_CMS check)
  [ ] SQL injection attempts fail (quote_smart)
  [ ] XSS attempts escaped (htmlspecialchars on all user data output)
  [ ] Test XSS payloads: <script>alert('XSS')</script> in all input fields
  [ ] User data displays as text, not executable code
  [ ] CSRF tokens present in all forms
  [ ] CSRF tokens verified in all POST handlers
  [ ] File upload validation working (if applicable)
  [ ] Upload directory has .htaccess protection
  [ ] Non-admin users can't access admin pages
  [ ] Invalid IDs don't cause errors
  [ ] Form resubmission prevented (redirects)

COMPATIBILITY:
-------------
  [ ] Works with basic theme
  [ ] Works with aurora theme
  [ ] Works with other custom themes
  [ ] Mobile responsive
  [ ] Cross-browser (Chrome, Firefox, Safari, Edge)
  [ ] Light and dark modes

PERFORMANCE:
-----------
  [ ] Page loads in < 2 seconds
  [ ] Database queries optimized
  [ ] No N+1 query problems
  [ ] Assets (CSS/JS) load properly
  [ ] No console errors in browser

================================================================================
11. DEPLOYMENT CHECKLIST
================================================================================

PRE-DEPLOYMENT:
--------------
[ ] All code commented and documented
[ ] No debugging code (console.log, var_dump, etc.)
[ ] Error handling in place
[ ] SQL tested and optimized
[ ] All files have proper permissions
[ ] Security measures implemented:
    [ ] XSS protection: htmlspecialchars() on all user data output
    [ ] SQL injection: quote_smart() on all database queries
    [ ] CSRF tokens in ALL forms
    [ ] CSRF verification in ALL POST handlers
    [ ] File uploads use validate_upload() function
    [ ] Upload directories have .htaccess protection
[ ] Cross-browser testing complete
[ ] Mobile testing complete

PLUGIN FILES:
------------
[ ] variable.php: All settings correct
[ ] sql/install.sql: Tables properly defined
[ ] All index.html files present
[ ] README.txt included
[ ] DEVELOPER_GUIDE.txt included
[ ] CSS and JS files minified (optional)

TESTING:
-------
[ ] Fresh install tested
[ ] Activation works without errors
[ ] All features functional
[ ] Deactivation clean (optional table deletion)
[ ] No conflicts with other plugins
[ ] No theme conflicts

DOCUMENTATION:
-------------
[ ] Installation instructions
[ ] Usage guide
[ ] Admin documentation
[ ] Version number updated
[ ] Changelog maintained

POST-DEPLOYMENT:
---------------
[ ] Monitor for errors
[ ] Check user feedback
[ ] Plan updates/improvements
[ ] Document known issues

================================================================================
11. TROUBLESHOOTING
================================================================================

PLUGIN WON'T ACTIVATE:
---------------------
- Check variable.php syntax
- Verify table names match between variable.php and install.sql
- Check SQL syntax in install.sql
- Look for PHP errors in logs
- Ensure database user has CREATE TABLE permissions

NO CONTENT DISPLAYS:
-------------------
- Check if IN_CMS constant blocking access
- Verify authentication permissions
- Check database queries for errors
- Look for PHP errors/warnings
- Verify Smarty variable assignments

CSS/JS NOT LOADING:
------------------
- Check $scriptAdd syntax in main plugin file
- Verify file paths are correct
- Check file permissions
- Clear browser cache
- Check browser console for 404 errors

DATABASE ERRORS:
---------------
- Use $db->sql_error() to see error message
- Verify quote_smart() used for all parameters
- Check SQL syntax
- Ensure fields exist in tables

ADMIN FEATURES NOT WORKING:
--------------------------
- Verify admin authentication included
- Check $form_action is set correctly
- Verify template/page.htm exists
- Check for PHP errors
- Verify $_POST data being received

PERMISSION ERRORS:
-----------------
- Check $page_lvl array configuration
- Verify group permissions in database
- Check authentication.php include
- Verify user's group has access

================================================================================
12. ADVANCED TOPICS
================================================================================

AJAX OPERATIONS:
---------------
Create separate PHP file for AJAX endpoints:
  ajax.php:
    <?php
    define('IN_CMS', true);
    require_once('../../../common.php');

    if ($_POST['action'] == 'load_data') {
        // Query database
        // Return JSON
        header('Content-Type: application/json');
        echo json_encode($data);
        exit;
    }

JavaScript:
    fetch('plugins/helloworld/ajax.php', {
        method: 'POST',
        body: JSON.stringify({ action: 'load_data' })
    })
    .then(response => response.json())
    .then(data => {
        // Update UI
    });

FILE UPLOADS:
------------
Form:
  enctype="multipart/form-data" in form tag

Processing:
  if (isset($_FILES['file'])) {
      $file = $_FILES['file'];
      $target = "uploads/" . basename($file['name']);

      // Validate
      $allowed = array('jpg', 'jpeg', 'png', 'gif');
      $ext = pathinfo($file['name'], PATHINFO_EXTENSION);

      if (in_array(strtolower($ext), $allowed)) {
          if (move_uploaded_file($file['tmp_name'], $target)) {
              // Success
          }
      }
  }

PAGINATION:
----------
Calculate:
  $per_page = 20;
  $page = isset($_GET['page']) ? (int)$_GET['page'] : 1;
  $offset = ($page - 1) * $per_page;

Query:
  $sql = "SELECT * FROM table LIMIT $offset, $per_page";

Links:
  $total_pages = ceil($total_records / $per_page);
  for ($i = 1; $i <= $total_pages; $i++) {
      echo '<a href="?page=' . $i . '">' . $i . '</a>';
  }

SCHEDULED TASKS:
---------------
Create cron.php in plugin directory:
  <?php
  define('IN_CMS', true);
  require_once('../../common.php');

  // Perform scheduled task
  // Update database, send emails, etc.

Setup cron job:
  0 0 * * * php /path/to/plugins/helloworld/cron.php

MULTI-LANGUAGE:
--------------
Define language constants in language files:
  plugins/helloworld/languages/english/lang.php:
    define('HELLO_GREETING', 'Hello');
    define('HELLO_FAREWELL', 'Goodbye');

Use in code:
  echo HELLO_GREETING;

PLUGIN UPDATES:
--------------
Create sql/update/ directory with versioned SQL files:
  sql/update/1.0.0-to-1.1.0.sql

Check version and run updates:
  $current = get_plugin_version('helloworld');
  if (version_compare($current, '1.1.0', '<')) {
      run_update_sql('1.0.0-to-1.1.0.sql');
      update_plugin_version('helloworld', '1.1.0');
  }

================================================================================
CONCLUSION
================================================================================

This Hello World plugin demonstrates all essential aspects of FlintmanCMS
plugin development. Use it as a template, reference, and learning tool for
creating your own custom plugins.

Key takeaways:
- Follow the established directory structure
- Implement proper security measures
- Use database abstraction layer
- Leverage Smarty for templating
- Provide both frontend and admin interfaces
- Document your code thoroughly
- Test extensively before deployment

For additional help:
- Study the portfolio plugin for more complex examples
- Review CMS core files for available functions
- Check FlintmanCMS documentation
- Join the developer community

Happy coding!

================================================================================
